#include <stdio.h>
double a_n(int n, double p){
    double a_i=p;
    printf("a_1=%lf",a_i);
    for(int i=2; i<=n; i++){
        a_i=0.5*(a_i+p\a_i);
        printf("a_%d=%lf",a_i);
        }    
    return a_i;
}

int main (){
    int n;
    double p;
    printf("")
}


// i have no clue è tipo uguale a quello di prima 
//ok dare nome alla funzione, okay dare argomenti, poi apri la graffa e scrivi dentro cosa deve fare la funzione, poi prima di uscire dalla funz le faccio tornare il valore che volgio io come soluzione della funzione. poi entro in sta main but what is main? e dento al main scrivo what?